#include <iostream>
#include <string>
using namespace std;

string provjera(int a, int b)
{
	if (a < 12)
	{
		return "AM";
	}
	else
	{
		return "PM";
	}
}


int main()
{
	int a;
	int b;
	cout << "Upisi vrijeme (sati minute) ";
	cin >> a >> b;
	if (a < 0 || a >= 24 || b < 0 || b >= 60)
	{
		cout << "neispravan unos";
	}
	else
	{

	}
	if(b == 0)
	{
		cout << "Vrijeme je " << a << ":" << "00" << " " << provjera(a, b) << endl;
	}
	else {
		cout << "Vrijeme je " << a << ":" << b << " " << provjera(a, b) << endl;
	}
		return 0;
}