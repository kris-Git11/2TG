#include <bits/stdc++.h>
using namespace std;

int main ()
{
    int x, y;
   for (int i=0; i<5; i++)
   {
    cin >> x >> y;
    if (y<x)
    {
        swap (x,y);
    }
   if (x>9 && x<100 && y>9 && y<100 && abs(a-b)<20)
   {
       for (int j=x; j<=y; j++)
        cout << j << " ";
   }
   else
    cout << x << " " << y;
    cout << endl;
  }


  return 0;
}